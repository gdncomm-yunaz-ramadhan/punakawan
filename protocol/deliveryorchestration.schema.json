{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/schema/protocol/deliveryorchestration.schema.json",
  "title": "DeliveryOrchestration",
  "description": "One continuous multi-project delivery run (punokawan-14yn.1). Owns zero or more DeliveryLanes and a list of not-yet-routed requirement inputs. State is derived by replaying its DeliveryEvent log, never written directly. See affiliate-platform-delivery-feedback-2026-08-07.md.",
  "type": "object",
  "required": ["id", "status", "unresolved_inputs", "created_at", "updated_at", "revision"],
  "additionalProperties": false,
  "properties": {
    "id": { "type": "string", "description": "Filesystem-safe ULID (Crockford base32, 26 chars)." },
    "status": { "type": "string", "enum": ["pending", "active", "cancelled", "completed"] },
    "unresolved_inputs": {
      "type": "array",
      "description": "Requirement sources (Jira/Confluence/GitHub/URL/free-text) not yet routed to a project. Routing and normalization is punokawan-14yn.2's concern; this task only persists the raw reference.",
      "items": {
        "type": "object",
        "required": ["reference"],
        "additionalProperties": false,
        "properties": {
          "reference": { "type": "string" },
          "note": { "type": "string" }
        }
      }
    },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" },
    "revision": { "type": "integer", "minimum": 0, "description": "Optimistic-concurrency counter; incremented on every applied event." }
  }
}
