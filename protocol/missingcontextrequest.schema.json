{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/schema/protocol/missingcontextrequest.schema.json",
  "title": "MissingContextRequest",
  "description": "A subagent's request for context its capsule did not include, routed back to Semar per punakawan-architecture-enhancement-plan.md §6.4. Punakawan never decides how to resolve it (ADR-0016) - it only persists the request and whichever resolution the calling agent, acting as Semar, chooses.",
  "type": "object",
  "required": ["id", "capsule_id", "query", "reason", "blocking", "status", "created_at"],
  "additionalProperties": false,
  "properties": {
    "id": { "type": "string" },
    "capsule_id": { "type": "string" },
    "query": { "type": "string" },
    "reason": { "type": "string" },
    "preferred_types": { "type": "array", "items": { "type": "string" } },
    "blocking": { "type": "boolean" },
    "status": {
      "description": "pending until Semar resolves it. §6.4's four resolutions: search for the context (not itself a terminal status - that's just a search_knowledge call, which then leads to one of the below), add it to a new capsule revision, reject it as irrelevant, or ask the user.",
      "type": "string",
      "enum": ["pending", "added_to_revision", "rejected", "asked_user"]
    },
    "resolution_note": { "type": "string" },
    "revised_capsule_id": {
      "description": "Set when status is added_to_revision: the id of the new ContextCapsule (from request_capsule) that supersedes capsule_id with the missing context included.",
      "type": "string"
    },
    "created_at": { "type": "string", "format": "date-time" },
    "resolved_at": { "type": "string", "format": "date-time" }
  }
}
