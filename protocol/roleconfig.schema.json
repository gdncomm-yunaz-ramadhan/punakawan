{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/schema/protocol/roleconfig.schema.json",
  "title": "RolePreferences",
  "description": "Per-project prompt preferences for the four Punakawan roles (Semar coordinates, Gareng challenges, Petruk plans and builds, Bagong verifies). Persisted at .punakawan/roles.yaml. Style selects one of three fixed prompt-guidance strings appended to a role's prompt; instructions is free text appended after that guidance. This configuration only shapes prompt wording: it never authorizes tools, grants permissions, or changes what a workflow requires. revision is bumped on every save for optimistic locking.",
  "type": "object",
  "required": ["version", "revision", "roles"],
  "additionalProperties": false,
  "properties": {
    "version": { "type": "integer", "const": 2 },
    "revision": { "type": "integer", "minimum": 0 },
    "roles": {
      "type": "object",
      "required": ["semar", "gareng", "petruk", "bagong"],
      "additionalProperties": false,
      "properties": {
        "semar": { "$ref": "#/$defs/rolePreference" },
        "gareng": { "$ref": "#/$defs/rolePreference" },
        "petruk": { "$ref": "#/$defs/rolePreference" },
        "bagong": { "$ref": "#/$defs/rolePreference" }
      }
    }
  },
  "$defs": {
    "rolePreference": {
      "type": "object",
      "required": ["style", "instructions"],
      "additionalProperties": false,
      "properties": {
        "style": {
          "type": "string",
          "description": "Selects one of three fixed prompt-guidance strings appended to the role's prompt. Never changes tool access or workflow requirements.",
          "enum": ["strict", "balanced", "creative"]
        },
        "instructions": {
          "type": "string",
          "description": "Free-text guidance appended after the fixed style guidance, bounded to 2000 characters.",
          "maxLength": 2000
        }
      }
    }
  }
}
