{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/schema/protocol/workflow.schema.json",
  "title": "WorkflowRun",
  "description": "A running or completed workflow instance and its state-machine position. See punakawan-go-typescript-detailed-plan.md §9, §18.1.",
  "type": "object",
  "required": ["id", "workspace", "workflow_name", "state", "created_at", "updated_at"],
  "additionalProperties": false,
  "properties": {
    "id": { "type": "string" },
    "workspace": { "type": "string" },
    "workflow_name": {
      "type": "string",
      "enum": [
        "feature-delivery",
        "requirement-review",
        "browser-flow-capture",
        "implementation-only",
        "final-review"
      ]
    },
    "state": {
      "type": "string",
      "enum": [
        "created",
        "context-building",
        "awaiting-clarification",
        "planning",
        "awaiting-approval",
        "executing",
        "reviewing",
        "blocked",
        "completed",
        "failed",
        "cancelled"
      ]
    },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" },
    "objective": {
      "type": "string",
      "description": "Human-readable goal of this run, set by the calling agent at creation or advance time. Used by the panel's session summary (punakawan-panel-implementation-plan.md §8.3); Punakawan never infers or edits this itself."
    },
    "initiator": {
      "type": "string",
      "description": "Who or what started this run (e.g. \"user\", \"scheduled\", an agent identifier). Set by the calling agent, not inferred."
    },
    "active_role": {
      "type": "string",
      "enum": ["semar", "gareng", "petruk", "bagong"],
      "description": "The Punakawan role currently driving this run, as reported by the calling agent."
    },
    "checkpoints": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["state", "at"],
        "additionalProperties": false,
        "properties": {
          "state": { "type": "string" },
          "at": { "type": "string", "format": "date-time" },
          "note": { "type": "string" }
        }
      }
    },
    "definition_ref": {
      "type": "object",
      "description": "Immutable reference to the workflow definition this run was created from (agent-context plan §4.1). Absent for ad hoc runs, which record their path through capability events and the outcome instead of pretending a definition existed. Present for definition-aware runs so the run records the exact id, revision, and content hash that produced it — replacing the old convention of encoding the definition id inside `objective`.",
      "required": ["id", "revision", "content_hash"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "revision": { "type": "integer" },
        "content_hash": { "type": "string", "description": "sha256:<hex> fingerprint of the definition content at invocation time." }
      }
    },
    "inputs": {
      "type": "object",
      "description": "Resolved workflow inputs at invocation time (agent-context plan §4.1/§4.4): declared inputs with caller-supplied values or declared defaults applied. Values are arbitrary JSON.",
      "additionalProperties": true
    },
    "plan_ref": {
      "type": "object",
      "description": "Immutable reference to the internal/plan revision this run was instantiated from (project hygiene refactor plan §5.3's workflow -> plan -> delivery pipeline). Absent for a run predating this wiring. Present for a run created by invoking a workflow definition, mirroring definition_ref's shape.",
      "required": ["id", "revision"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "revision": { "type": "integer" }
      }
    },
    "context_snapshot": {
      "type": "object",
      "description": "Bounded, immutable snapshot of the context this run was prepared with (agent-context plan §4.1). Holds references and hashes, never a copy of the whole knowledge store. Fully populated by the context preparation service (plan §4.4); at invocation only the `missing` list and revision may be set.",
      "additionalProperties": false,
      "properties": {
        "prepared_at": { "type": "string", "format": "date-time" },
        "digest": { "type": "string", "description": "sha256:<hex> digest covering the definition reference, selected metadata, selected knowledge references and hashes, role-config revision, and inputs." },
        "project_metadata_revision": { "type": "integer" },
        "metadata": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["key", "reason"],
            "additionalProperties": false,
            "properties": {
              "key": { "type": "string" },
              "value": {},
              "reason": { "type": "string" }
            }
          }
        },
        "knowledge": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["id", "reason"],
            "additionalProperties": false,
            "properties": {
              "id": { "type": "string" },
              "content_hash": { "type": "string" },
              "validity": { "type": "string" },
              "reason": { "type": "string" }
            }
          }
        },
        "missing": {
          "type": "array",
          "description": "Context the run needs but could not resolve. A non-empty list drives the run into awaiting-clarification (plan §4.1).",
          "items": {
            "type": "object",
            "required": ["kind"],
            "additionalProperties": false,
            "properties": {
              "kind": { "type": "string", "description": "e.g. \"metadata\", \"knowledge\", \"input\"." },
              "key": { "type": "string" }
            }
          }
        }
      }
    },
    "step_progress": {
      "type": "array",
      "description": "Per-step execution state initialized from the definition's steps at invocation and advanced as steps run (agent-context plan §4.1/§5.3). Absent for ad hoc runs.",
      "items": {
        "type": "object",
        "required": ["step_id", "state"],
        "additionalProperties": false,
        "properties": {
          "step_id": { "type": "string" },
          "state": {
            "type": "string",
            "enum": ["pending", "ready", "running", "completed", "blocked", "skipped"]
          },
          "evidence_ids": {
            "type": "array",
            "items": { "type": "string" }
          },
          "deviation_reason": {
            "type": "string",
            "description": "Why this step deviated from the definition (e.g. a different capability was actually used). Recorded on completion in place of, or alongside, evidence (plan §5.3)."
          }
        }
      }
    },
    "outcome": {
      "type": "object",
      "description": "The structured result of a context-aware run (agent-context plan §6.1). A context-aware run cannot enter `completed` without one. An observation here is a traceable input to a learning proposal, not yet canonical knowledge.",
      "required": ["status"],
      "additionalProperties": false,
      "properties": {
        "status": { "type": "string", "enum": ["success", "partial", "failed"] },
        "summary": { "type": "string" },
        "recorded_at": { "type": "string", "format": "date-time" },
        "evidence_ids": { "type": "array", "items": { "type": "string" } },
        "output_refs": { "type": "array", "items": { "type": "string" } },
        "deviations": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["step_id", "reason"],
            "additionalProperties": false,
            "properties": {
              "step_id": { "type": "string" },
              "reason": { "type": "string" },
              "actual_capability": { "type": "string" }
            }
          }
        },
        "missing_context": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["kind"],
            "additionalProperties": false,
            "properties": {
              "kind": { "type": "string" },
              "key": { "type": "string" }
            }
          }
        },
        "observations": {
          "type": "array",
          "description": "Reusable lessons the agent identified, each classified for a later proposal (plan §6.2). Not canonical until reviewed and accepted.",
          "items": {
            "type": "object",
            "required": ["kind", "summary"],
            "additionalProperties": false,
            "properties": {
              "kind": { "type": "string", "description": "workflow | metadata | knowledge | contradiction | workflow-revision (plan §6.2)." },
              "summary": { "type": "string" },
              "evidence_ids": { "type": "array", "items": { "type": "string" } }
            }
          }
        }
      }
    },
    "role_config_revision": {
      "type": "integer",
      "description": "The roles.yaml revision in effect when this run was created (plan §50, ROLE-012). Stamped once at creation so a historical run remains reproducible even after the project role configuration is later edited."
    },
    "effective_role_settings": {
      "type": "object",
      "description": "Snapshot of the effective role settings (enabled/style/mode/capabilities) for each of the four roles at run-creation time (plan §50, ROLE-012). A map keyed by role name; values are permissive objects so the snapshot stays forward-compatible with future role-config fields.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": true
      }
    }
  }
}
