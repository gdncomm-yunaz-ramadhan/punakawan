{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/schema/protocol/panelsourcehealth.schema.json",
  "title": "PanelSourceHealth",
  "description": "Health/availability of one data source (Dolt, BD, Git, an adapter, ...) backing a workspace's panel view, per punakawan-panel-implementation-plan.md §9's WorkspaceAvailability enum. Failure of one source must not fail the whole workspace view.",
  "type": "object",
  "required": ["source", "availability", "checked_at"],
  "additionalProperties": false,
  "properties": {
    "source": { "type": "string" },
    "availability": {
      "type": "string",
      "enum": ["available", "partially_available", "busy", "unavailable", "invalid"]
    },
    "message": { "type": "string" },
    "checked_at": { "type": "string", "format": "date-time" }
  }
}
