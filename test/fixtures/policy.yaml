capabilities:
  filesystem:
    read:
      - "workspace/**"
    write:
      - "workspace/**"
    deny:
      - "**/.env"
      - "**/.ssh/**"
      - "**/secrets/**"

  git:
    commit: allowed
    push: allowed
    force_push: denied
    default_branch_write: denied

  external:
    jira_read: allowed
    jira_write: allowed
    confluence_read: allowed
    confluence_write: allowed

  browser:
    controlled_profile: allowed
    existing_profile: allowed
    record_inputs: sanitized

  execution:
    network: restricted
    timeout_seconds: 600
    max_output_bytes: 1000000
