/* eslint-disable */
/**
 * Code generated from protocol/*.schema.json. DO NOT EDIT.
 * Regenerate with `pnpm --filter @punakawan/schema-types generate`.
 */

import { z } from "zod"

export const ApprovalManifestSchema = z.object({ "id": z.string().describe("Filesystem-safe ULID (Crockford base32, 26 chars)."), "orchestration_id": z.string(), "project_id": z.string(), "parent_task_ids": z.array(z.string()).describe("Every ParentTask this manifest covers; all must already be routed to project_id."), "planned_base_ref": z.string(), "planned_branches": z.array(z.string()).optional(), "expects_jira_writes": z.boolean().default(false), "expects_commits": z.boolean().default(false), "expects_pushes": z.boolean().default(false), "expects_prs": z.boolean().default(false), "checks": z.array(z.object({ "name": z.string(), "status": z.enum(["pass","fail","skipped"]), "classification": z.enum(["required","optional","delegated-to-ci"]), "detail": z.string().optional() }).strict()).describe("Snapshot of the PreflightCheck results (preflightcheck.schema.json) this manifest was computed against, duplicated inline rather than by $ref - this repo does not use cross-file $ref (see knowledge.schema.json's selector comment) and a manifest's checks are a point-in-time copy, not a live reference anyway."), "status": z.enum(["pending","approved","rejected"]), "approved_by": z.string().describe("Never one of the agent role identifiers (semar/gareng/petruk/bagong) - an agent may not approve its own manifest.").optional(), "decided_at": z.string().datetime({ offset: true }).optional(), "created_at": z.string().datetime({ offset: true }), "revision": z.number().int().gte(0).describe("Optimistic-concurrency counter; incremented on every applied event."), "proposed_worklog_total_hours": z.number().describe("Total verified-work hours (internal/worklogalloc), computed from this project's completed test-run command durations, that the proposed worklog below is derived from. Zero when no test-run evidence has accumulated yet for this manifest's parent tasks - proposed worklogs must be visible before project approval.").optional(), "proposed_worklog": z.array(z.object({ "bucket": z.enum(["dev","test","review"]), "subtask_key": z.string().describe("The configured Jira subtask (from list_jira_subtasks) this bucket's hours would be logged against."), "subtask_name": z.string().optional(), "hours": z.number() }).strict()).describe("The dev/test/review split of proposed_worklog_total_hours that could be matched to one of this project's configured Jira subtasks by name (internal/worklogalloc.Allocate) - a proposal for update_jira_task_progress to post post-approval, not something this manifest itself writes to Jira. Coarse by construction: internal/testrun records only that a command ran and how long it took, with no dev/test/review distinction, so the total is split evenly across whichever buckets have a matching subtask, never a fabricated precise breakdown.").optional(), "proposed_worklog_unmapped_hours": z.number().describe("Hours from proposed_worklog_total_hours that could not be matched to any configured subtask (no subtask name contained a recognized dev/test/review keyword) - left unmapped rather than guessed onto an arbitrary subtask.").optional() }).strict().describe("One immutable approval request/decision for a single project within a DeliveryOrchestration: the parent tasks it covers, the planned base ref/branches/writes, and the preflight checks it was computed against. Approving one manifest never authorizes a newly discovered project or a write category beyond what this manifest declares - a changed scope requires a new manifest, not an edit to this one.")
export type ApprovalManifestSchema = z.infer<typeof ApprovalManifestSchema>
