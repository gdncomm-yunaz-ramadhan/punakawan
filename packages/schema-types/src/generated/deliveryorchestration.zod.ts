/* eslint-disable */
/**
 * Code generated from protocol/*.schema.json. DO NOT EDIT.
 * Regenerate with `pnpm --filter @punakawan/schema-types generate`.
 */

import { z } from "zod"

export const DeliveryOrchestrationSchema = z.object({ "id": z.string().describe("Filesystem-safe ULID (Crockford base32, 26 chars)."), "title": z.string().describe("Short human-readable summary of what this run delivers, as last set by whoever started or edited it. Absent when nobody supplied one - and absent on every run created before titles existed - so a consumer that needs a label always derives one from the run's requirement references instead of reading this field directly.").optional(), "description": z.string().describe("Longer prose about what this run is for and why it exists, as last set by whoever started or edited it. Absent when nobody wrote one; unlike title, nothing derives a substitute, because prose nobody wrote would be invention rather than description.").optional(), "project_ids": z.array(z.string()).describe("Projects explicitly attached to this run, in attachment order. Attachment is a deliberate statement that a run involves a project, kept separate from the incidental fact that some lane happens to name it: a project can be attached before it has any lane, and detaching it never touches lanes it already owns.").optional(), "plan_record_id": z.string().describe("Id of the knowledge record holding this run's final plan, as persisted by submit_final_plan. Absent until a plan has been recorded against the run.").optional(), "session_id": z.string().describe("Id of the workflow run driving this delivery - the same id sessions are identified by everywhere else (a WorkflowRun's id, which is also PanelSessionSummary's id and the run_id the MCP tools thread). Absent when no session has claimed the run.").optional(), "status": z.enum(["pending","active","cancelled","completed"]), "unresolved_inputs": z.array(z.object({ "reference": z.string(), "note": z.string().optional() }).strict()).describe("Requirement sources (Jira/Confluence/GitHub/URL/free-text) not yet routed to a project. Routing and normalization happens elsewhere; this record only persists the raw reference."), "created_at": z.string().datetime({ offset: true }), "updated_at": z.string().datetime({ offset: true }), "revision": z.number().int().gte(0).describe("Optimistic-concurrency counter; incremented on every applied event."), "workflow_definition_id": z.string().describe("Id of a saved workflow definition this run was configured from, if any. When set, its per-role restrictions decide which of the four role stages a lane must complete before its lease can be marked done, in place of the default of requiring all four. Absent when no such definition was attached.").optional() }).strict().describe("One continuous multi-project delivery run. Owns zero or more DeliveryLanes and a list of not-yet-routed requirement inputs. State is derived by replaying its DeliveryEvent log, never written directly.")
export type DeliveryOrchestrationSchema = z.infer<typeof DeliveryOrchestrationSchema>
