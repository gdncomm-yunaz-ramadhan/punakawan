/* eslint-disable */
/**
 * Code generated from protocol/*.schema.json. DO NOT EDIT.
 * Regenerate with `pnpm --filter @punakawan/schema-types generate`.
 */

import { z } from "zod"

export const ReviewConclusionSchema = z.object({ "id": z.string().describe("Filesystem-safe ULID (Crockford base32, 26 chars)."), "lane_id": z.string(), "outcome": z.enum(["approved","changes_requested","blocked"]), "reviewer_worker_id": z.string(), "reviewer_session_id": z.string(), "reviewer_model": z.string().optional(), "reviewer_provider": z.string().optional(), "independence_level": z.enum(["same_session","different_session","different_worker"]).describe("How independent this reviewer was from whoever implemented the attempt being reviewed."), "independence_override_reason": z.string().describe("Required to record a conclusion whose reviewer_session_id matches the implementer's own session; absent otherwise.").optional(), "evidence_ids": z.array(z.string()).describe("EvidenceArtifact ids the reviewer's conclusion is grounded in."), "blocking_finding_ids": z.array(z.string()).describe("ReviewFinding ids that must be resolved before this conclusion could become approved; empty when the outcome is already approved."), "verification_matrix_computed_at": z.string().datetime({ offset: true }).describe("The computed_at of the VerificationMatrix the reviewer actually looked at - pins exactly what verification state this conclusion was formed against."), "recorded_at": z.string().datetime({ offset: true }) }).strict().describe("One reviewer's recorded outcome for a lane's current attempt, computed and stored the same way VerificationMatrix is (a scan of the lane's own event log, not a DeliveryLane field), since a lane may accumulate more than one conclusion across retries. independence_level and the reviewer's own worker/session/model/provider identity are recorded so a later reader can tell whether this conclusion came from a genuinely separate reviewer rather than the same session that implemented the change - recording the same session as reviewer is rejected unless an override reason is given.")
export type ReviewConclusionSchema = z.infer<typeof ReviewConclusionSchema>
